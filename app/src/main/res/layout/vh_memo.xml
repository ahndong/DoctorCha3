<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="beanMemo"
            type="com.infoline.doctorcha.presentation.bean.BeanMemo"/>

        <variable
            name="myBindClickListener"
            type="com.infoline.doctorcha.core.binding.MyBindClickListener"/>

        <variable
            name="myBindTextChangedListener"
            type="com.infoline.doctorcha.core.binding.MyBindTextChangedListener"/>
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:layout_marginTop="1dp"
        android:clickable="true"
        android:background="@android:color/white"
        android:foreground="?attr/selectableItemBackground">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tv_31"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="#0000ff"
                    android:ems="9"
                    android:text="@{beanMemo.ud}"/>

                <EditText
                    android:id="@+id/et_31"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="12dp"
                    android:ems="9"
                    android:inputType="textMultiLine"
                    android:text="@{beanMemo.mm}"
                    android:hint="메모 내용을 입력하세요"
                    android:background="@drawable/x_bg_editor_search"
                    android:minLines="3"
                    android:focusable="@{beanMemo.vhm == 0 ? false : true}"/>
            </LinearLayout>
            <!--
            //안먹는다
            1. android:onTextChanged="@{(s, start, before, count) -> myBindTextChangedListener.onBindTextChanged(s)}"
            2. android:onTextChanged="@{myBindTextChangedListener::onBindTextChanged}"-->

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/iv_modi_save"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@{beanMemo.vhm == 0 ? @drawable/ic_mode_edit_black_24dp : @drawable/ic_save_black_24dp}"
                    android:onClick="@{myBindClickListener::onBindClick}"/>

                <ImageView
                    android:id="@+id/iv_del_restore"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:src="@{beanMemo.vhm == 0 ? @drawable/ic_delete_forever_black_24dp : @drawable/ic_replay_black_24dp}"
                    android:onClick="@{myBindClickListener::onBindClick}"/>
            </LinearLayout>
        </LinearLayout>
    </FrameLayout>
</layout>